<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <template id="s4b_external_layout_standard_wohaf">

        <!-- Header -->
        <!-- Odoo Default header deleted, this layout is dedicated for the headerless version -->

        <!-- FixDiv -->
        <div t-attf-class="article o_report_layout_standard o_company_#{company.id}_layout {{  'o_layout_background' if company.layout_background in ['Geometric', 'Custom']  else  '' }}" t-attf-style="background-image: url({{ 'data:image/png;base64,%s' % company.layout_background_image.decode('utf-8') if company.layout_background_image and company.layout_background == 'Custom' else '/base/static/img/bg_background_template.jpg' if company.layout_background == 'Geometric' else ''}});" t-att-data-oe-model="o and o._name" t-att-data-oe-id="o and o.id" t-att-data-oe-lang="o and o.env.context.get('lang')">
            <div class="pt-5">
                <!-- This div ensures that the address is not cropped by the header. -->
                <t t-call="web.address_layout"/>
            </div>
            <t t-out="0"/>
        </div>
        
        <!-- Footer -->
        <!-- Odoo Default footer deleted, custom used in the Template itself, to provide the necessary look wich conforms the Romanian Style -->

    </template>

    <template id="s4b_external_layout_wohaf">
        <t t-if="not o" t-set="o" t-value="doc"/>

        <t t-if="not company">
            <!-- Multicompany -->
            <t t-if="company_id">
                <t t-set="company" t-value="company_id"/>
            </t>
            <t t-elif="o and 'company_id' in o and o.company_id.sudo()">
                <t t-set="company" t-value="o.company_id.sudo()"/>
            </t>
            <t t-else="else">
                <t t-set="company" t-value="res_company"/>
            </t>
        </t>

        <!-- Dynamic Layout selector disabled, so it won't change the layout for this specific invoice -->
        <t t-call="s4b_project_category.s4b_external_layout_standard_wohaf"> <t t-out="0"/> </t>

    </template>
</odoo>